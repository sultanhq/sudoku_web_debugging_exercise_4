<!DOCTYPE html>
<html>
  <head>
    <title>Sudoku</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <h1>Life is too short for playing Sudoku !</h1>
    <%= styled_flash %>
    <form action="/" method="post">
      <div class="board">
        <table cellspacing="0">
          <tbody>
            <tr>
            <% @cells.each_with_index { |cell, i| if i % 9.0 == 0 && i != 0 %>
            </tr><tr><td class="cell">
              <input type='text' name="cells[]"  value= <%= cell.to_s%> >
            </td>
            <% else %>

            <td class="cell">
              <input type='text' name="cells[]"  value= <%= cell.to_s%> >
            </td>
            <% end } %>

          </tr>
          </tbody>
        </table>
      </div>
      <input type="submit" value="check">
    </form>
  

    <a href="/new">New Game</a>
  
  </body>
</html>

